//seperate the individual character from string.
#include<stdio.h>
#include<string.h>
int main()
{
    char s[20];
    printf("\nenter a string:");
    gets(s);
    for(int i=0; i<s[i]; i++)
    {
        printf("\t%c", s[i]);
    }
return 0;
}